# excel_uart_tool
This app transfers calibration values directly from an .xlsx file to a device using a UART connection. . After the transfer, the app also allows users to verify the parameters on the calibrated device
